{
  "description": "Returns the details of an existing External Secrets Manager instance.",
  "path": "/v1/external-secrets-managers/{esmId}",
  "method": "GET",
  "parameters": [
    {
      "name": "esmId",
      "in": "path",
      "required": true,
      "description": "The unique identifier of the External Secrets Manager instance.",
      "schema": {
        "type": "string"
      }
    }
  ]
}