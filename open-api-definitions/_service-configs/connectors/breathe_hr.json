{
  "type": "object",
  "properties": {
    "config": {
      "type": "object",
      "properties": {
        "api_key": {
          "title": "",
          "type": "string",
          "description": "Your Breathe HR API key.",
          "format": "password"
        },
        "environment": {
          "type": "object",
          "description": "Your Breathe HR environment.",
          "enum": [
            "api.breathehr.com",
            "api.sandbox.breathehr.info"
          ]
        },
        "schema": {
          "title": "Destination schema name",
          "type": "string",
          "description": "Destination schema name. Schema name is permanent and cannot be changed after connection creation"
        }
      },
      "description": "",
      "required": [
        "schema"
      ]
    }
  },
  "x-sources": [
    "breathe_hr_config_V1",
    "schema_format_schema"
  ]
}