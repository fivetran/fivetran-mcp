{
  "type": "object",
  "properties": {
    "config": {
      "type": "object",
      "properties": {
        "accounts": {
          "title": "",
          "type": "array",
          "description": "Specific Accounts to sync.  Must be populated if `sync_mode` is set to `SpecificAccounts`.",
          "items": {
            "title": "",
            "type": "string",
            "description": ""
          }
        },
        "secret": {
          "title": "",
          "type": "string",
          "description": "The Twilio API secret",
          "format": "password"
        },
        "sid": {
          "title": "",
          "type": "string",
          "description": "The Twilio API key SID"
        },
        "sync_mode": {
          "title": "",
          "type": "object",
          "description": "Whether to sync all accounts or specific accounts.",
          "enum": [
            "AllAccounts",
            "SpecificAccounts"
          ]
        },
        "schema": {
          "title": "Destination schema name",
          "type": "string",
          "description": "Destination schema name. Schema name is permanent and cannot be changed after connection creation"
        }
      },
      "description": "",
      "required": [
        "schema"
      ]
    },
    "external_secrets_keys_config": {
      "type": "object",
      "properties": {
        "secret": {
          "title": "",
          "type": "string",
          "description": "ESM key for the Twilio API secret"
        },
        "sid": {
          "title": "",
          "type": "string",
          "description": "ESM key for the Twilio API key SID"
        }
      },
      "description": ""
    }
  },
  "x-sources": [
    "twilio_config_V1",
    "schema_format_schema",
    "twilio_esm_keys_config_V1"
  ]
}