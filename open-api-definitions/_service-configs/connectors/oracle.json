{
  "type": "object",
  "properties": {
    "config": {
      "type": "object",
      "properties": {
        "always_encrypted": {
          "title": "",
          "type": "boolean",
          "description": "Require TLS through Tunnel"
        },
        "connection_type": {
          "title": "",
          "type": "object",
          "description": "Possible values:`Directly`, `PrivateLink`, `SshTunnel`. `SshTunnel` is used as a value if this parameter is omitted in the request and any of the following parameter's values is specified: `tunnel_host`, `tunnel_port`, `tunnel_user`. Otherwise, `Directly` is used as a value if the parameter is omitted.",
          "enum": [
            "Directly",
            "PrivateLink",
            "SshTunnel",
            "ProxyAgent"
          ]
        },
        "database": {
          "title": "",
          "type": "string",
          "description": "The database name."
        },
        "host": {
          "title": "",
          "type": "string",
          "description": "DB instance host or IP address."
        },
        "password": {
          "title": "",
          "type": "string",
          "description": "The user's password.",
          "format": "password"
        },
        "pdb_name": {
          "title": "",
          "type": "string",
          "description": "<strong>Optional:</strong> Required only for containerized database."
        },
        "port": {
          "title": "",
          "type": "integer",
          "description": "The port number.",
          "format": "int32"
        },
        "tunnel_host": {
          "title": "",
          "type": "string",
          "description": "SSH host, specify only to connect via an SSH tunnel (do not use a load balancer)."
        },
        "tunnel_port": {
          "title": "",
          "type": "integer",
          "description": "SSH port, specify only to connect via an SSH tunnel.",
          "format": "int32"
        },
        "tunnel_user": {
          "title": "",
          "type": "string",
          "description": "SSH user, specify only to connect via an SSH tunnel."
        },
        "update_method": {
          "title": "",
          "type": "object",
          "description": "The method used to detect new or changed rows. Supported values: - `BINARY_LOG_READER` - Fivetran reads Oracle redo logs to detect modified rows in the source tables. - `TELEPORT` - Fivetran's proprietary replication method that uses compressed snapshots to detect and apply changes.",
          "enum": [
            "BINARY_LOG_READER",
            "LOGMINER",
            "TELEPORT"
          ]
        },
        "user": {
          "title": "",
          "type": "string",
          "description": "The user name."
        }
      },
      "description": ""
    },
    "external_secrets_keys_config": {
      "type": "object",
      "properties": {
        "password": {
          "title": "",
          "type": "string",
          "description": "ESM key for the user's password."
        },
        "user": {
          "title": "",
          "type": "string",
          "description": "ESM key for the user name."
        }
      },
      "description": ""
    }
  },
  "x-sources": [
    "oracle_config_V1",
    "oracle_esm_keys_config_V1"
  ]
}