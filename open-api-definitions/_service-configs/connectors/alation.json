{
  "type": "object",
  "properties": {
    "config": {
      "type": "object",
      "properties": {
        "client_id": {
          "title": "",
          "type": "string",
          "description": "Client ID of the OAuth client application registered in Alation for this connection."
        },
        "client_secret": {
          "title": "",
          "type": "string",
          "description": "Client secret of the OAuth client application registered in Alation for this connection.",
          "format": "password"
        },
        "subdomain": {
          "title": "",
          "type": "string",
          "description": "Your Alation base URL."
        },
        "schema": {
          "title": "Destination schema name",
          "type": "string",
          "description": "Destination schema name. Schema name is permanent and cannot be changed after connection creation"
        }
      },
      "description": "",
      "required": [
        "schema"
      ]
    }
  },
  "x-sources": [
    "alation_config_V1",
    "schema_format_schema"
  ]
}