{
  "type": "object",
  "properties": {
    "config": {
      "type": "object",
      "properties": {
        "api_token": {
          "title": "",
          "type": "string",
          "description": "Your Pylon Support Platform API token.",
          "format": "password"
        },
        "region": {
          "type": "object",
          "description": "The region of your Pylon Support Platform account. If your account is in the EU (tokens generated at app.eu.usepylon.com), provide `api.eu`. Otherwise provide `api`.",
          "enum": [
            "api.eu",
            "api"
          ]
        },
        "schema": {
          "title": "Destination schema name",
          "type": "string",
          "description": "Destination schema name. Schema name is permanent and cannot be changed after connection creation"
        }
      },
      "description": "",
      "required": [
        "schema"
      ]
    }
  },
  "x-sources": [
    "pylon_support_platform_config_V1",
    "schema_format_schema"
  ]
}