{
  "type": "object",
  "properties": {
    "config": {
      "type": "object",
      "properties": {
        "api_key": {
          "title": "",
          "type": "string",
          "description": "Your Healthie API key.",
          "format": "password"
        },
        "environment": {
          "type": "object",
          "description": "Your Healthie environment.",
          "enum": [
            "staging-api",
            "api"
          ]
        },
        "shard_authorization_id": {
          "title": "",
          "type": "string",
          "description": "Your Healthie shard authorization ID.",
          "format": "password"
        },
        "schema": {
          "title": "Destination schema name",
          "type": "string",
          "description": "Destination schema name. Schema name is permanent and cannot be changed after connection creation"
        }
      },
      "description": "",
      "required": [
        "schema"
      ]
    }
  },
  "x-sources": [
    "healthie_config_V1",
    "schema_format_schema"
  ]
}