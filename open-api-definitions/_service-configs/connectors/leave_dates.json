{
  "type": "object",
  "properties": {
    "config": {
      "type": "object",
      "properties": {
        "personal_access_token": {
          "title": "",
          "type": "string",
          "description": "Your Leave Dates personal access token.",
          "format": "password"
        },
        "schema": {
          "title": "Destination schema name",
          "type": "string",
          "description": "Destination schema name. Schema name is permanent and cannot be changed after connection creation"
        }
      },
      "description": "",
      "required": [
        "schema"
      ]
    }
  },
  "x-sources": [
    "leave_dates_config_V1",
    "schema_format_schema"
  ]
}