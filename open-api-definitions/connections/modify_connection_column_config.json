{
  "description": "⚠️ WRITE OPERATION - Confirm with user before calling. Updates the column config within your table for an existing connection within your Fivetran account.\n\nFor the NetSuite SuiteAnalytics and Salesforce and Salesforce Sandbox connectors, the 'schemas' map field will always have a single entry with the 'netsuite' or 'salesforce' key, respectively.\n> NOTE: The response contains all known schemas and tables. Also, it contains columns whose state has ever been set by the user. For more information, see also the [Connection Schema config](https://fivetran.com/docs/rest-api/tutorials/connection-schema-configuration-use-cases) tutorial.\n",
  "path": "/v1/connections/{connectionId}/schemas/{schemaName}/tables/{tableName}/columns/{columnName}",
  "method": "PATCH",
  "parameters": [
    {
      "name": "connectionId",
      "in": "path",
      "required": true,
      "description": "The unique identifier of the connection. Retrieve it from the `id` field in the [List All Connections](https://fivetran.com/docs/rest-api/api-reference/connections/list-connections) response, or from the `id` field returned when you [Create a Connection](https://fivetran.com/docs/rest-api/api-reference/connections/create-connection).",
      "schema": {
        "type": "string"
      }
    },
    {
      "name": "schemaName",
      "in": "path",
      "required": true,
      "description": "The schema name as stored in the connection schema config. This value is case-sensitive; an incorrect case results in an HTTP 404 error.",
      "schema": {
        "type": "string"
      }
    },
    {
      "name": "tableName",
      "in": "path",
      "required": true,
      "description": "The table name as stored in the connection schema config. This value is case-sensitive; an incorrect case results in an HTTP 404 error.",
      "schema": {
        "type": "string"
      }
    },
    {
      "name": "columnName",
      "in": "path",
      "required": true,
      "description": "The column name as stored in the connection schema config. This value is case-sensitive; an incorrect case results in an HTTP 404 error.",
      "schema": {
        "type": "string"
      }
    }
  ],
  "request_body_schema": {
    "required": [
      "enabled"
    ],
    "type": "object",
    "properties": {
      "enabled": {
        "type": "boolean",
        "description": "The boolean value specifying whether the sync for the table into the destination is enabled."
      },
      "hashed": {
        "type": "boolean",
        "description": "The boolean value specifying whether a column should be hashed"
      },
      "is_primary_key": {
        "type": "boolean"
      }
    }
  }
}