{
  "description": "⚠️ WRITE OPERATION - Confirm with user before calling. Rotates the API key for the specified service account and generates a new secret.\n\nDuring the optional overlap period, both the old and new secrets remain valid, allowing zero-downtime credential rotation.\n\n> Note: Only a regular user can perform this operation.\n",
  "path": "/v1/users/{userId}/api-keys/rotate",
  "method": "POST",
  "parameters": [
    {
      "name": "userId",
      "in": "path",
      "required": true,
      "description": "The unique identifier for the user within the account.",
      "schema": {
        "type": "string"
      }
    }
  ],
  "request_body": {
    "content": {
      "application/json": {
        "type": "object",
        "properties": {
          "expiration_period_days": {
            "type": "integer",
            "description": "The number of days until the API key expires. Defaults to 90. Maximum is 365.",
            "format": "int32"
          },
          "overlap_days": {
            "type": "integer",
            "description": "The number of days the old and new API keys both remain valid after rotation. Maximum is 30 days.",
            "format": "int32"
          }
        }
      }
    }
  },
  "response": {
    "content": {
      "application/json": {
        "required": [
          "code",
          "data"
        ],
        "properties": {
          "code": {
            "type": "string",
            "description": "Response status code"
          },
          "message": {
            "type": "string",
            "description": "Response status text"
          },
          "data": {
            "required": [
              "expires_at",
              "key_secret",
              "key_status",
              "key_value"
            ],
            "type": "object",
            "properties": {
              "key_value": {
                "type": "string",
                "description": "The public identifier of the API key."
              },
              "key_secret": {
                "type": "string",
                "description": "The secret portion of the API key. Only returned at creation time and cannot be retrieved again."
              },
              "key_status": {
                "type": "string",
                "description": "The status of the API key. Possible values: `ACTIVE`: The key has a single active secret, `ACTIVE_GRACE`:  The key has two active secrets during rotation overlap, and `EXPIRED`: The key has expired.",
                "enum": [
                  "ACTIVE",
                  "ACTIVE_GRACE",
                  "EXPIRED"
                ]
              },
              "expires_at": {
                "type": "string",
                "description": "The timestamp when the API key expires.",
                "format": "date-time"
              },
              "overlap_expires_at": {
                "type": "string",
                "description": "The timestamp when the previous secret expires after rotation. Only present during the overlap period.",
                "format": "date-time"
              },
              "last_rotated_at": {
                "type": "string",
                "format": "date-time"
              }
            }
          }
        }
      }
    },
    "description": "Successful response"
  }
}